[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "example-package-hollandjg"
version = "0.1.0"
dependencies = [
  "numpy"
]

[project.optional-dependencies]
test = ["pytest"]